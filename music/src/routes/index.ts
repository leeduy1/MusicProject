import Home from '~/pages/Home'
import App from '~/App'
import Profile from '~/pages/Profile'

const publicRoutes = [
  { path: '/', component: Home }
  { path: '/profile', component: Profile },

]

export { publicRoutes }
